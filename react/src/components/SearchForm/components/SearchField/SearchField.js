import React from 'react';
import PropTypes from 'prop-types';

const SearchField = () => {
  return <div className="container">
    <input className="form form-control" type="text" placeholder="Enter your query..." />
  </div>
}

export default SearchField;